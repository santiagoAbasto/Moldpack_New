@extends('layouts.site')
@section('body_class', 'quality-route')
@section('content')
@php($section=$page->sections->firstWhere('type','quality_page') ?? $page->sections->first())
@php($settings=$section?->settings ?? [])
@php($photo=$section?->media?->first())
<div class="quality-page"><nav class="quality-breadcrumb shell" aria-label="Migas de pan"><a href="/">Inicio</a><span aria-hidden="true"> &gt; </span><span>Calidad</span></nav>@if($section)<section class="quality-main shell" aria-labelledby="quality-title"><div class="quality-copy"><h1 id="quality-title">{{ $section->title }}</h1><div class="quality-intro">{!! $section->body !!}</div><div class="quality-points">@foreach($section->items as $item)<article><img src="{{ asset('assets/figma/exact/calidad/check-circle.svg') }}" alt="" width="20" height="20"><div><strong>{{ $item->title }}:</strong> {!! $item->body !!}</div></article>@endforeach</div></div><figure class="quality-photo"><img src="{{ asset($photo?->path ?: 'assets/figma/exact/calidad/raw-1.png') }}" alt="{{ $photo?->alt ?: 'Pirotines Moldpack de colores' }}" width="601" height="600"></figure></section><section class="quality-downloads shell" aria-labelledby="quality-downloads-title"><h2 id="quality-downloads-title">{{ $settings['downloads_title'] ?? 'Descargas' }}</h2>@if(!empty($settings['document_path']))<a class="quality-download" href="{{ asset($settings['document_path']) }}" download><img src="{{ asset('assets/figma/exact/logo-header.png') }}" alt=""><span>{{ $settings['document_label'] ?? 'Política de calidad' }}</span><img class="quality-download-icon" src="{{ asset('assets/figma/exact/calidad/download.svg') }}" alt="Descargar" width="24" height="24"></a>@else<div class="quality-download disabled"><img src="{{ asset('assets/figma/exact/logo-header.png') }}" alt=""><span>{{ $settings['document_label'] ?? 'Política de calidad' }}</span><img class="quality-download-icon" src="{{ asset('assets/figma/exact/calidad/download.svg') }}" alt="" width="24" height="24"></div>@endif</section>@endif</div>
@endsection
